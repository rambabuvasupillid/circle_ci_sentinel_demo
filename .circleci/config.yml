  version: 2.1
  jobs:
    build:
      docker: 
        - image: circleci/node:4.8.2 # the primary container, where your job's commands are run
      steps:
        - checkout # check out the code in the project directory
        - run: ifconfig ##checking ip of local image
        - run: sudo apt install wget -y
        - run: sudo apt install unzip -y
        - run: wget https://releases.hashicorp.com/vault/1.3.0/vault_1.3.0_linux_amd64.zip
        - run: unzip vault_1.3.0_linux_amd64.zip;ls -la
        - run: sudo chmod u+x vault; sudo mv vault /usr/local/bin/;sudo ls -la /usr/local/bin/
